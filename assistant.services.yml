services:
  assistant:
    class: Drupal\assistant\Assistant
    arguments: ['@path.current', '@entity_type.manager', '@date.formatter', '@renderer', '@image.factory']

  taxonomy_term_tree:
    class: Drupal\assistant\TaxonomyTermTree
    arguments: ['@entity_type.manager']
